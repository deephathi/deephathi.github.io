# Deephathi Blog

This repository contains a simple Jekyll site. To run the site locally you will need Ruby and Bundler installed.

## Install dependencies
```bash
bundle install
```

## Serve the site locally
```bash
bundle exec jekyll serve
```

Then visit `http://localhost:4000` in your browser.
